pipeline{
    agent any
    stages{
        stage('CONFIG'){
            steps{
                git branch: 'main', credentialsId: 'github', url: 'https://github.com/Priyanka-nataraju/pipeline-repo.git'
            }
        }
    }
}